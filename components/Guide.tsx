import React from "react";
import { VerticalView } from "app/styled-components";

export default function Guide(name: string) {
  const navigation = {
    // key = current route
    // value = {prev:null, next: null }
  };

  // return (

  // )
}
